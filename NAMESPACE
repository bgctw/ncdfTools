exportPattern("^[^\\.]")

#import(aNamespace)	# only required when extending S3 methods 

#useDynLib(ProjectsDllBaseName)	# only required when supplying C or FORTRAN code 

import(RNetCDF)
importFrom(chron, chron, as.chron, years)
importFrom(DistributionUtils, logHist)
importFrom(abind, abind)
import(foreach)
import(doMC)
importFrom(raster, raster)
importFrom(jannis.misc, labelMargins,  GetVecInfo, image.rotated, plot.bg,
           plot.nlines, userCoords, RangeZeroEqui, IndVector2Array,
           which.closest, IndVectors2IndMatrix)
importFrom(spectral.methods, MEF, RMSE, isSeriesContinuous, isSeriesConstant,
           printStatus, gapfillSSA, filterTSeriesSSA, summarizeTemporalGaps)
importFrom(RColorBrewer, brewer.pal)
import(snowfall)
import(snow)
importFrom(plotrix, color.legend)
